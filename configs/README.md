# Executable configurations required

The locked JSON protocols in this folder document the experiment. Add executable YAML/JSON configs consumed by the original Python code, with no machine-specific absolute paths.
